export enum RoleDescriptionsEnum {
  ADMIN = 'Administrator: Highest level; manages users, settings, and system access; can do everything.',
  USER = 'Regular User: Standard access; can use application features but has limited administrative capabilities.',
  VIEWER = 'Viewer: Read-only access; can view content but cannot make changes or manage settings.',
  EDITOR = 'Editor: Can create and modify content; has more permissions than a regular user but cannot manage users or settings.',
}

